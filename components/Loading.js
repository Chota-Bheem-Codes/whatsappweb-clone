import { Circle } from "better-react-spinkit"

const Loading = () => {
    return (
        <div style={{display:'grid',placeItems:'center',height:'100vh'}}>
            <Circle color="#3CBC28" size='300px'/>
        </div>
    )
}

export default Loading
